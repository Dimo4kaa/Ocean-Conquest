export class Ship {
    constructor(size, direction, x, y) {
        this.killed = false;
        this.size = size;
        this.direction = direction;
        this.x = x;
        this.y = y;
    }
}
